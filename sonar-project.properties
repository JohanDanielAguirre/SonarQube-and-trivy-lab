# SonarQube/SonarCloud project configuration
# NOTE: Keep this file in UTF-8 without BOM and plain ASCII comments to avoid encoding issues on Windows.

# Core project identity (override from CLI if needed)
sonar.projectKey=qualifica-o-seu-professor1
sonar.projectName=qualifica-o-seu-professor
sonar.projectVersion=1.0.0

# SonarQube local URL (if you use SonarCloud, override via CLI: -Dsonar.host.url=https://sonarcloud.io)
sonar.host.url=http://localhost:9000

# Authentication (local token) - WARNING: do not commit real tokens to public repos
sonar.token=sqp_452c252fe32ae0c0f379ef003c7e9742a797e951
# Legacy fallback for older scanners (optional):
sonar.login=sqp_452c252fe32ae0c0f379ef003c7e9742a797e951

# Encoding for source files
sonar.sourceEncoding=UTF-8

# Source and test directories
sonar.sources=src
sonar.tests=src/__tests__

# Consider tests colocated with source as tests too
sonar.test.inclusions=src/**/*.test.ts,src/**/*.spec.ts,src/**/__tests__/**/*.ts

# Exclusions from code analysis
sonar.exclusions=dist/**,coverage/**,node_modules/**,src/scripts/**,src/types/**

# Coverage report produced by Jest (lcov)
sonar.javascript.lcov.reportPaths=coverage/lcov.info

# Help the TS analyzer find the tsconfig
sonar.typescript.tsconfigPath=tsconfig.json
